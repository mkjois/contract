function f(x, y, z) {
  /**
   * #contract: x is number
   * #setup: my_z = ["a", {one: 1}]
   * #example: f(5, null, my_z) => 120
   */
  if (x < 2) {
    return 1;:q
  }
  return x * f(x-1);
}

function (a,b) {
  // yolo
  // swag
  // money
  return 5;
}
